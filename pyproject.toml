[project]
name = "dashborion-backend"
version = "1.0.0"
description = "Dashborion Backend API"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend"]
